export const STORAGE_KEYS = Object.freeze({
  token: 'bms_token',
  theme: 'bms_theme',
});

export const USER_ROLES = Object.freeze({
  admin: 'admin',
  manager: 'manager',
  cashier: 'cashier',
});

